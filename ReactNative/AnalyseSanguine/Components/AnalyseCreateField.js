import React from "react";
import { useEffect } from "react";
import {
    View,
    Text,
    Button,
    StyleSheet,
    FlatList,
    TextInput,
    Touchable,
    TouchableHighlight,
    TouchableOpacity,
    ScrollView,
} from "react-native";
const AnalyseCreateField = (props) => {
    return (
        <View>
            {props.typeAnalyse}
        </View>
    )
}